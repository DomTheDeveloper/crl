name: Verify Lean proofs

# One-shot queue cleanup for the stale PR #63 run.

on:
  push:
    branches:
      - agent/checkerboard-publication-package
    paths:
      - ".github/workflows/verify-lean.yml"

concurrency:
  group: checkerboard-lean-Verify Lean proofs-refs/pull/63/merge
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  clear-stale-queue:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Attempted stale general Lean PR 63 queue cleanup."
