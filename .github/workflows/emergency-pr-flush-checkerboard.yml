name: Emergency PR flush checkerboard queue

on:
  pull_request:
    types: [opened, reopened, synchronize]

concurrency:
  group: Verify checkerboard Lean proofs
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  flush-only:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Scheduler-only queue flush"
