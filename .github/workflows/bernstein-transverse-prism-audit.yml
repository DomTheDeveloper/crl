name: Bernstein transverse prism Lean audit

on:
  push:
    branches:
      - formal/bernstein-transverse-prism-v1
    paths:
      - 'math/bernstein_obstacle/lean/**'
      - '.github/workflows/bernstein-transverse-prism-audit.yml'
  pull_request:
    paths:
      - 'math/bernstein_obstacle/lean/**'
      - '.github/workflows/bernstein-transverse-prism-audit.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: bernstein-transverse-prism-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4

      - name: Install pinned Lean
        shell: bash
        run: |
          set -euo pipefail
          curl -fsSL https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh \
            | sh -s -- -y --default-toolchain none
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"

      - name: Download pinned mathlib cache
        working-directory: math/bernstein_obstacle/lean
        run: lake exe cache get

      - name: Build full library
        working-directory: math/bernstein_obstacle/lean
        run: lake build BernsteinObstacle

      - name: Audit transverse-prism endpoints
        working-directory: math/bernstein_obstacle/lean
        shell: bash
        run: |
          set -euo pipefail
          lake env lean TransversePrismSaturationAudit.lean 2>&1 | tee transverse-prism-axioms.log
          if grep -q 'sorryAx' transverse-prism-axioms.log; then
            echo 'Unexpected sorryAx found' >&2
            exit 1
          fi
          grep -F 'BernsteinObstacle.transversePrism_fiberIntegral_lowerBound' transverse-prism-axioms.log
          grep -F 'BernsteinObstacle.transversePrism_localEnergy_lowerBound' transverse-prism-axioms.log

      - name: Upload audit transcript
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bernstein-transverse-prism-audit
          path: math/bernstein_obstacle/lean/transverse-prism-axioms.log
          if-no-files-found: warn
