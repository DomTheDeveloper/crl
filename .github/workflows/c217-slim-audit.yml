name: Slim C217 Lean audit

on:
  push:
    branches:
      - audit/c217-slim-checker
  workflow_dispatch:

concurrency:
  group: c217-focused-audit
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-slim
    steps:
      - run: echo 'Cancellation run complete; C217 proof branch remains preserved.'
