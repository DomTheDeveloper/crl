name: A263135 Windows scratch audit

on:
  pull_request:
    paths:
      - '.github/workflows/verify-wowii322-pr.yml'

permissions:
  contents: read

concurrency:
  group: a263135-windows-scratch
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-slim
    steps:
      - run: echo 'Cancellation run; A263135 proof branch is preserved.'
