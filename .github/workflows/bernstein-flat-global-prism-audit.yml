name: Bernstein flat global prism Lean audit

on:
  push:
    branches:
      - formal/bernstein-flat-global-prism-v1
    paths:
      - 'math/bernstein_obstacle/lean/**'
      - '.github/workflows/bernstein-flat-global-prism-audit.yml'
  pull_request:
    paths:
      - 'math/bernstein_obstacle/lean/**'
      - '.github/workflows/bernstein-flat-global-prism-audit.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: bernstein-flat-global-prism-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
      - name: Install pinned Lean
        shell: bash
        run: |
          set -euo pipefail
          curl -fsSL https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh | sh -s -- -y --default-toolchain none
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
      - name: Download pinned mathlib cache
        working-directory: math/bernstein_obstacle/lean
        run: lake exe cache get
      - name: Build flat global prism endpoint
        working-directory: math/bernstein_obstacle/lean
        shell: bash
        run: |
          set -euo pipefail
          lake build BernsteinObstacle.FlatGlobalPrism 2>&1 | tee flat-global-prism-build.log
          lake build BernsteinObstacle 2>&1 | tee -a flat-global-prism-build.log
      - name: Audit flat global prism endpoints
        working-directory: math/bernstein_obstacle/lean
        shell: bash
        run: |
          set -euo pipefail
          lake env lean FlatGlobalPrismAudit.lean 2>&1 | tee flat-global-prism-axioms.log
          if grep -q 'sorryAx' flat-global-prism-axioms.log; then exit 1; fi
          grep -F 'BernsteinObstacle.flatInterface_prismPatch_lowerSq' flat-global-prism-axioms.log
          grep -F 'BernsteinObstacle.flatInterface_hilbertVI_sharp_from_prismPatch' flat-global-prism-axioms.log
          grep -F 'BernsteinObstacle.flatInterface_hilbertVI_sharp_of_finPatch' flat-global-prism-axioms.log
      - name: Upload audit evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bernstein-flat-global-prism-audit
          path: |
            math/bernstein_obstacle/lean/flat-global-prism-build.log
            math/bernstein_obstacle/lean/flat-global-prism-axioms.log
          if-no-files-found: warn
