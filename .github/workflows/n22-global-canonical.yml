name: Global canonical n22 search

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  parked:
    name: Global monolith parked
    runs-on: ubuntu-latest
    steps:
      - run: echo "Monolithic search parked; the exhaustive sharded matrix is authoritative."
