name: Emergency checkerboard queue flush

on:
  issue_comment:
    types: [created]

concurrency:
  group: Verify checkerboard Lean proofs
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  flush-only:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Scheduler-only concurrency flush"
