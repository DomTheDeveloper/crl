name: One-shot A263135 exact audit

on:
  pull_request:
    paths:
      - '.github/workflows/tmp-voronovskaja-one-shot.yml'
      - '.github/workflows/a263135-external-check.yml'

permissions:
  contents: read

concurrency:
  group: one-shot-a263135-${{ github.ref }}
  cancel-in-progress: true

jobs:
  audit:
    runs-on: ubuntu-slim
    timeout-minutes: 180
    steps:
      - name: Checkout Formal Conjectures workbench
        uses: actions/checkout@v6
        with:
          repository: DomTheDeveloper/formal-conjectures
          ref: dtd/oeis-a263135-scd-scratch
          path: formal-conjectures
      - name: Install pinned Lean
        shell: bash
        run: |
          set -euo pipefail
          curl -sSfL https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh \
            | sh -s -- -y --default-toolchain none
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
      - name: Fetch mathlib cache
        working-directory: formal-conjectures
        shell: bash
        run: |
          set -euo pipefail
          lake exe cache get
      - name: Check A263135 workbench
        working-directory: formal-conjectures
        shell: bash
        run: |
          set -euo pipefail
          lake env lean Scratch/A263135Rows.lean 2>&1 | tee "$RUNNER_TEMP/a263135-audit.log"
      - name: Upload transcript
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: a263135-one-shot-audit
          path: ${{ runner.temp }}/a263135-audit.log
          if-no-files-found: warn
