name: One-shot WOWII 59 exact audit

on:
  pull_request:
    paths:
      - '.github/workflows/tmp-voronovskaja-one-shot.yml'

permissions:
  contents: read

concurrency:
  group: one-shot-wowii-59-${{ github.ref }}
  cancel-in-progress: true

jobs:
  audit:
    runs-on: ubuntu-slim
    steps:
      - run: echo 'Cancellation run; the verified WOWII 59 proof branch is preserved.'
