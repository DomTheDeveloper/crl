name: Verify WotW316 proof

on:
  push:
    paths:
      - "proofs/lean/WotW316/**"
      - ".github/workflows/verify-wotw316.yml"
  pull_request:
    paths:
      - "proofs/lean/WotW316/**"
      - ".github/workflows/verify-wotw316.yml"
  workflow_dispatch:

concurrency:
  group: wotw316-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lean-wotw316:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
      - name: Install elan
        run: |
          curl -sSfL https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh | sh -s -- -y --default-toolchain none
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
      - name: Guard against placeholders
        run: "! grep -RnE '(^|[[:space:]])(sorry|admit|native_decide)([[:space:]]|$)' proofs/lean/WotW316/*.lean"
      - name: Resolve dependencies
        working-directory: proofs/lean/WotW316
        run: lake update
      - name: Fetch cache
        working-directory: proofs/lean/WotW316
        run: lake exe cache get || echo "cache miss"
      - name: Build and kernel-check
        working-directory: proofs/lean/WotW316
        shell: bash
        run: |
          set -o pipefail
          lake build 2>&1 | tee build.log
      - name: Upload diagnostic
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: wotw316-build-log
          path: proofs/lean/WotW316/build.log
          if-no-files-found: warn
      - name: Reject sorryAx
        if: success()
        working-directory: proofs/lean/WotW316
        run: |
          if grep -q "sorryAx" build.log; then
            echo "::error::Proof depends on sorryAx"; exit 1
          fi
