name: Verify checkerboard Lean proofs

on:
  push:
    branches:
      - claude/gracious-hamilton-8k6xhz

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  flush-only:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Scheduler-only queue flush"
