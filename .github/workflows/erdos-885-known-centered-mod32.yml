name: Erdős 885 known-centered mod-thirty-two lift

on:
  pull_request:
    paths:
      - 'research/erdos885/sage_known_centered_lift.py'
      - '.github/workflows/erdos-885-known-centered-mod32.yml'
  workflow_dispatch:

concurrency:
  group: erdos-885-known-centered-mod32
  cancel-in-progress: true

jobs:
  lift:
    runs-on: ubuntu-latest
    timeout-minutes: 240
    steps:
      - uses: actions/checkout@v4
      - run: docker pull sagemath/sagemath:latest
      - name: Lift exact known mod-sixteen parents to mod thirty-two
        run: |
          mkdir -p results
          docker run --rm -v "$PWD:/work" -w /work sagemath/sagemath:latest \
            sage -python research/erdos885/sage_known_centered_lift.py 16
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: erdos-885-known-centered-mod32
          path: results/known_centered_mod32.json
          if-no-files-found: warn
