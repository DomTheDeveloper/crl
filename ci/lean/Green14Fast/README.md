# Green14 fast kernel audit

This isolated project pins the Green14 proof branch. The workflow generates a direct Boolean-array checker for each certificate `t = 20, ..., 39`, compiles it with kernel `decide`, prints the theorem's axiom footprint, and rejects compiler-trust axioms.
